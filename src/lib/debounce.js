export const debounce = (callback, delay) => {
	// let timeoutId;
	// return wrapper;
	// function wrapper(...args) {
	// 	if (timeoutId) {
	// 		clearTimeout(timeoutId);
	// 	}
	// 	timeoutId = setTimeout(() => {
	// 		timeoutId = null;
	// 		callback(...args);
	// 	}, delay);
	// }
};
